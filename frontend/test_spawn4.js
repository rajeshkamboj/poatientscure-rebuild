const { spawn } = require("child_process"); const child = spawn("powershell.exe", ["-Command", "Write-Host \\"hello\\""]); child.stdout.on("data", (data) => { process.stdout.write(data); }); child.stderr.on("data", (data) => { process.stderr.write(data); }); child.on("close", (code) => { process.exit(code); });
